# 03 动态对象映射器

## 详解

按同名同类型属性，用 Emit 生成 `User -> UserDto` 映射委托，等价于手写逐字段赋值，但只需生成一次。

## 运行后你会看到什么

1. `User` 映射为 `UserDto`（`Age` 不在 Dto 中，自动跳过）
2. 使用静态缓存的映射委托

## 注意点

- `new TTarget()` + 逐属性 get/set + `Ret`
- 映射委托应缓存，不要每次映射都重新 Emit
