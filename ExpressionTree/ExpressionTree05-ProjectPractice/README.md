# 05 项目实践

## 场景

这个示例模拟一个用户查询接口：

- 前端传入筛选条件 DTO
- 服务层动态组装表达式树
- 仓储层接收 `Expression<Func<User, bool>>`
- 最后对数据源执行过滤

## 设计意义

这和真实项目里 Repository + ORM 的使用方式非常接近：

- 服务层负责组合业务条件
- 仓储层负责接收查询表达式
- ORM 负责把表达式树翻译成 SQL

这里我们用内存数据模拟最后一步。
