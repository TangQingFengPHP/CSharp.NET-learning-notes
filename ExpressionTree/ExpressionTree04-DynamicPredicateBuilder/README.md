# 04 动态条件拼接

## 详解

表达式树在业务里最常见的用途之一，就是“根据用户输入动态拼条件”。

例如一个搜索页面：

- 名称可选
- 最低价格可选
- 是否上架可选

这时你不可能手写所有组合分支，最自然的方式就是动态构建 `Expression<Func<T, bool>>`。

## 关键点

- 参数必须复用同一个 `ParameterExpression`
- 多个条件用 `AndAlso` / `OrElse` 组合
- 常量和成员访问都要显式构建
