# 01 创建线程、传参与获取结果

## 详解

`Thread` 创建后必须 `Start()` 才会运行，同一实例只能启动一次。没有 `Thread<T>`，返回值需通过共享变量 + `Join()` 同步。

## 运行后你会看到什么

1. 主线程与工作线程并发执行
2. Lambda 捕获订单参数
3. `ParameterizedThreadStart` 传 `object` 参数
4. 共享变量接收计算结果

## 注意点

- Lambda 捕获的是变量引用，不是创建时的快照
- 读取共享结果前必须先 `Join()`
