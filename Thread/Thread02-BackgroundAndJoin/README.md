# 02 前后台线程与 Join

## 详解

前台线程会阻止进程退出；后台线程在前台全部结束后可能被直接终止。`Join` 阻塞等待，`Join(timeout)` 超时后目标线程仍在运行。

## 运行后你会看到什么

1. `Join` 超时后继续等待线程完成
2. 后台线程行为说明
3. `Thread.Sleep` 与 `Task.Delay` 对比

## 注意点

- 不要依赖后台线程做关键收尾（日志、刷新、提交）
- 异步方法里用 `Task.Delay`，不要用 `Thread.Sleep`
