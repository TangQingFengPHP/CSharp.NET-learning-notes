# 06 源码分析

## 分析方式

这里不直接粘贴框架源码，而是用“简化版结构”理解 SignalR 的几个关键角色：

- `Hub`：通信入口
- `HubContext`：给业务层用的发送器
- `HubLifetimeManager`：管理连接与消息分发
- Dispatcher：负责把某次调用路由到正确的 Hub 方法

## 设计启发

SignalR 的源码设计很值得学，因为它体现了典型的框架式分层：

- 上层暴露抽象
- 中层组织调用流程
- 下层处理连接与传输细节

项目里写实时系统时，也可以用同样思路分层，而不是把所有逻辑都压到一个 Hub 里。
